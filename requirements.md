# 项目需求说明

## 1. 项目定位

本项目是个人静态博客，目标是简单、安全、维护成本低、容易迁移。

公开博客必须保持静态，当前技术路线是：

- Astro + TypeScript
- Markdown / MDX 写文章
- pnpm 管理依赖
- GitHub Actions 构建
- GitHub Pages 托管
- Cloudflare 管理 DNS、自定义域名、HTTPS 和前置保护

阿里云 ECS 只作为未来动态服务预留。当前公开博客不要引入 ECS、Docker、数据库、服务端运行时、WordPress、Ghost 或后端 API。

## 2. 网站信息

- 网站名称：`辣子鸡丁砂锅`
- 网站描述：`这个人懒死了，什么介绍都没有`
- 作者昵称：`辣子鸡丁砂锅`
- 语言：中文，用户可见文案默认使用简体中文
- 公开地址：`https://blog.20050619.xyz`
- GitHub 账号：`JiLiGUaLaaaaa`
- GitHub 仓库：`git@github.com:JiLiGuaLaaaaa/JiLiGuaLaaaaa.github.io.git`
- GitHub Pages 目标域名：`jiligualaaaaa.github.io`
- Cloudflare DNS：`blog CNAME jiligualaaaaa.github.io`

站点名称、描述、头像和图标必须集中配置，方便以后修改。

## 3. 安全规则

永远不要提交密钥或敏感信息。

禁止提交：

- ECS 登录信息、root 密码、公网 IP 与密码组合
- SSH 私钥
- Cloudflare API Token
- GitHub Token
- 包含真实密钥的 `.env`
- 数据库文件
- 私密日记内容
- 本地服务器登录信息
- 任何账号密码或真实服务连接信息

`.gitignore` 必须覆盖常见敏感文件：

```gitignore
.env
.env.*
*.pem
*.key
server-secret.txt
secrets.txt
private/
diary/
*.sqlite
*.db
相关配置信息.txt
```

`相关配置信息.txt` 是本地配置文件，不能读取、展示或提交。若发现真实敏感信息已经进入仓库，应立即停止并报告。

## 4. 页面与内容

公开站点需要保留：

- 首页
- 博客列表页
- 博客文章详情页
- 关于页
- 标签页
- 归档页
- 搜索页或搜索入口
- 404 页面
- RSS
- sitemap
- robots.txt

文章放在 `src/content/blog/`，使用 Markdown 或 MDX。推荐 frontmatter：

```yaml
title: "文章标题"
description: "文章描述"
pubDate: 2026-01-01
updatedDate: 2026-01-02
tags:
  - 示例标签
draft: false
```

文章公开路径必须使用 `/blog/xxx/`，不要改成 `/posts/xxx/`。生产环境不发布 `draft: true` 的文章。

## 5. 功能要求

必须保留或预留：

- 暗色模式
- RSS 订阅
- sitemap
- robots.txt
- 标签
- 归档
- 静态搜索，优先使用纯前端方案
- 右下角二次元小角色

当前不显示评论区，也不显示评论占位，不接入任何真实评论系统。未来评论、统计、表单、API、私密日记等动态能力应作为独立服务部署，不混入当前公开静态博客。

私密日记不得放进 GitHub Pages。不要创建 `/diary`、`/private`、`/private-diary` 等公开路由，除非用户明确确认这些内容可以公开。

## 6. 当前视觉设计

整体视觉参考 `uxiaohan/vhAstro-Theme` 的卡片、资料卡、两列布局和柔和 Banner 风格，但只迁移静态前端表现。

当前设计要求：

- 使用樱花粉色系和柔和渐变
- 页面基础背景使用简单柔和渐变，不再把 `public/images/site-bg.jpg` 作为博客页面背景图
- 顶部 Banner / Hero 使用 `public/images/banner/` 中的主题图片；前景图片应在保证主要人物完整可见的前提下尽可能占满整个 Banner，只有前景图因比例限制无法铺满时，才使用模糊背景层补足空白区域；前景图和模糊背景之间不能有生硬分界，应通过柔和透明过渡、边缘混合和遮罩融合；浅色模式下 Banner 图片不能被白色遮罩压得模糊发灰
- 电脑版 Banner 当前效果已确认，不应继续改动；移动端 Banner 可以单独优化，让横幅图在窄屏下尽可能填满可见区域，同时仍优先保证主要人物完整可见
- 博客、标签、标签详情、归档、搜索、关于等主题页的 Banner 在向下滚动时应统一流畅收起，让正文区域完整展示
- 即使页面正文内容很少，也要保留足够的页面可滚动空间，保证用户仍然可以向下滚动并触发 Banner 收起；不足内容用空白区域补齐，不用无意义内容填充
- 顶部导航栏应在用户向下滚动时收起，向上滚动时重新显示；页面回到顶部时必须显示；滚动到页面底部时不要因为浏览器滚动校正或回弹误触发显示；并尊重 `prefers-reduced-motion`
- 不再维护单独主界面；根路径 `/` 进入后应直接跳转到博客页 `/blog/`
- 文章列表卡片应支持整张卡片点击进入文章，卡片内标签链接仍应可以单独点击
- 移动端顶部导航应保持紧凑，暗色/亮色模式按钮不要单独占一整行
- 首屏加载应避免不必要的重资源抢占，例如全量动作帧立即预加载或高成本固定背景绘制
- 资料卡显示头像、文章数、标签数、热门标签快捷导航和公开 QQ
- 页脚 RSS/Sitemap 使用两组拼接复古徽章：`SEO + Sitemap`、`订阅 + RSS`
- 博客列表页顶部只保留标题，不显示旧说明文案
- 博客概览只显示文章数和标签数，不显示 `/blog/` 路径

当前公开资源：

- `public/images/site-icon.png`：网页图标
- `public/images/blog-avatar.png`：博客头像
- `public/images/banner/`：主题 Banner 图片
  - `blog.png`：博客页 Banner
  - `tags.png`：标签页和标签详情页 Banner
  - `archive.png`：归档页 Banner
  - `search.png`：搜索页 Banner
  - `about.png`：关于页 Banner
- `public/images/video-mascot/`：当前主用小人动作帧

不要再保留旧版小人帧、旧抠图、旧预览图、审查图或根目录重复素材作为仓库内容。

## 7. 二次元小角色

组件路径：`src/components/CursorCharacter.astro`。

当前小人使用 `public/images/video-mascot/` 中的透明 PNG 动作帧。当前动作设计已经确认满意，后续不要重新设计、重切、重生成或替换动作帧，除非用户明确要求。

必须保留：

- 鼠标移动时按角度切换视线方向
- 多角度方向帧
- 正面闲置眨眼
- 点击一次只播放一次挥手
- 拖动位置
- 隐藏 / 显示
- 移动端隐藏
- `prefers-reduced-motion`
- 图片预加载和双图片层切换，避免切帧闪烁

如果以后需要重新生成当前小人帧，使用：

- 本地源视频：`生成指定动作视频 (3).mp4`
- 处理脚本：`scripts/process_video_mascot.py`
- 工具环境：使用 `uv` 安装并运行 Python 依赖

生成脚本可以输出本地预览图和审查图，但这些临时产物不要提交。

小人说话文案在 `src/components/CursorCharacter.astro` 中维护：

- 初始气泡文字在 `data-character-bubble`
- 自动轮播文案在 `messages` 数组
- 重新显示时的文案在 `say("我回来啦。")`

这些文案会公开显示，不要写入任何敏感信息。

## 8. 部署规则

使用 GitHub Actions 部署到 GitHub Pages，工作流位于 `.github/workflows/deploy.yml`。

部署流程：

1. 安装 pnpm 和 Node
2. 执行 `pnpm install --frozen-lockfile`
3. 执行 `pnpm build`
4. 上传 `dist/`
5. 发布到 GitHub Pages

GitHub Pages 的 Source 必须选择 GitHub Actions，不要使用 Deploy from a branch。否则 GitHub Pages 会按 Jekyll 解析 Astro 源码并导致 YAML front matter 错误。

Astro 站点地址应为 `https://blog.20050619.xyz`。公开博客不要使用 ECS 或 Docker 部署。

## 9. 文档与计划规则

需求变化时按顺序处理：

1. 更新 `requirements.md`
2. 更新 `plan.md`
3. 实现代码或资源调整
4. 必要时更新 README
5. 运行检查
6. 完成后更新 plan 状态

`plan.md` 使用 Markdown checkbox，保留关键历史阶段和当前 TODO，不再记录过度冗长的逐轮细节。

README 使用简单中文说明：

- 项目是什么
- 如何本地运行
- 如何写文章
- 如何构建和部署
- GitHub Pages 和 Cloudflare 如何配置
- RSS、sitemap、标签、归档、搜索是什么
- 哪些文件不能提交
- 为什么当前公开博客不使用 ECS
- 当前主要资源放在哪里

## 10. 质量检查

完成任务前优先运行：

```bash
pnpm install
pnpm build
```

如果环境没有全局 `pnpm`，可使用：

```bash
corepack pnpm install --frozen-lockfile
corepack pnpm build
```

如果包含可用检查，也应运行：

- `tsc --noEmit`
- `git diff --check`
- 敏感词或敏感文件扫描

如果 `pnpm build` 在当前沙箱环境因 Astro/Vite `spawn EPERM` 失败，要明确说明，并在本机或 GitHub Actions 再验证。

## 11. 项目整理规则

保持仓库轻量：

- 不提交 `dist/`、`.astro/`、`node_modules/`、`.venv/`、`.uv-cache/`、`.pnpm-store/`
- 不提交视频源文件、预览图、审查图、Python `__pycache__` 或临时处理产物
- 不保留已被当前实现替代的旧资源和旧脚本
- 删除文件前必须确认不被当前代码、构建流程或后续维护脚本引用

## 12. 非目标

除非用户明确修改需求，否则不要实现：

- WordPress
- Ghost
- 服务端渲染博客
- 数据库 CMS
- 用户登录系统
- 公开博客后台管理系统
- 当前仓库中的动态后端 API
- 当前公开博客的 Docker 或 ECS 部署
- 把私密日记放进 GitHub Pages

## 13. 本次新增动态功能需求

用户本次明确要求使用带公网 IP 的私人小电脑为博客补充动态能力，但公开博客本体仍继续使用 GitHub Pages 托管静态页面。

新增动态能力必须作为独立服务实现：

- 静态博客继续由 Astro 构建并发布到 GitHub Pages。
- 独立动态服务服务器运行单独的动态服务，用于日记、动态记录和访问量统计等 API。
- 静态博客通过可配置的 API 基础地址访问独立动态服务服务器。
- API 不可用时，公开博客页面必须仍能正常浏览，只降级隐藏或提示动态数据不可用。
- 动态服务域名、端口、密钥、服务器地址、账号密码和 Token 不得写入仓库。
- 仓库只允许提交 `.env.example` 这类假占位配置。

需要实现或预留的功能：

- 连续点击右下角互动小人多次后，显示或跳转到写日记入口。
- 写日记入口只能作为隐藏入口，不是安全边界；真正写入必须经过动态服务认证。
- 支持创建私密日记草稿，私密日记不得进入 GitHub Pages 发布目录。
- 支持发布公开的个人生活记录，公开记录可以由博客前端读取展示。
- 支持访问量统计，至少包括全站总访问量和当前页面访问量。
- 访问量统计应尽量少采集个人信息，不记录不必要的隐私数据。
- 公开前端应提供生活记录展示入口和访问量展示入口。

安全和隐私边界：

- 服务器配置文件只可用于本地部署判断，不得复制到代码、文档、日志、提交信息或回复中。
- 不提交真实数据库文件、访问日志、私密日记内容或服务器登录信息。
- 私密日记必须经过服务端认证访问。
- 管理类 API 必须要求认证；公开读取 API 只能返回已发布的公开生活记录和聚合访问量。
- CORS 只允许博客域名和本地开发地址，具体域名通过环境变量配置。

域名与部署边界：

- 公开博客域名继续指向 GitHub Pages。
- 动态服务应使用独立 API 子域名或独立服务地址，不与 GitHub Pages 构建产物混在一起。
- 域名、DNS、反向代理和 HTTPS 配置需要由用户在 Cloudflare、服务器或控制台中最终确认。
- 不在仓库中写入真实服务器公网 IP、真实 SSH 用户、真实密码、证书私钥或 Cloudflare API Token。

## 14. 本次动态发布与日记交互升级

用户希望把“生活”改成更明确的“动态”页面，并让动态发布像 QQ 空间发说说一样简单。

新增交互要求：

- 公共展示页面使用“动态”命名，不再继续把它作为“生活记录”对外展示。
- 动态页需要提供直接发布公开动态的输入框。
- 发布动态时必须经过密码确认，密码正确后才能发布。
- 动态内容可以很简短，像“说说”一样只写文本即可，标题不是必填。
- 日记入口需要通过右下角小人触发。
- 触发条件当前为 8 秒内连续点击交互小人 20 次。
- 达到触发条件后，先进入密码确认环节。
- 密码正确后才进入日记编辑环节。
- 日记编辑同样需要走动态服务，不得写回 GitHub Pages 静态目录。
- 动态页和日记页都应在 API 不可用时显示明确的降级提示，而不是静默失败。

## 15. 本次动态入口体验修复

用户确认博客域名已经恢复正常，当前需要继续修复动态服务接入和前端交互体验。

新增调整要求：

- 8 秒内连续点击交互小人 20 次后，必须先打开日记密码弹窗；即使动态服务地址暂未配置，也应在弹窗里提示服务地址缺失，而不是完全不弹窗。
- 动态页面发布框不再显示“心情”输入项。
- 动态发布密码不再常驻显示在页面上；用户点击“发布”后再弹出密码输入确认，确认后提交发布。
- 动态服务地址必须通过 `PUBLIC_DYNAMIC_API_BASE` 注入到 GitHub Pages 构建产物；GitHub Actions 需要显式读取仓库 Actions Variable。
- 动态发布密码和日记密码继续只在独立动态服务服务器环境变量中配置，不写入前端代码、GitHub Pages 构建产物或仓库文档。

## 16. 本次私人服务器部署迁移需求

用户当前不再使用阿里云 ECS，改用带公网 IP 的私人小电脑作为动态服务服务器。动态服务部署方案必须保持可移植，后续迁移到其他服务器时不需要重写代码。

新增要求：

- 文档和脚本应统一使用“独立动态服务服务器”表述，兼容私人 Linux 服务器、ECS 或其他云服务器。
- 提供可在目标 Linux 服务器上执行的一键安装脚本，用于配置 Node 动态服务、数据目录、systemd 后台服务和 nginx 反向代理。
- 一键安装脚本应优先复用已有 Node.js 18+，并支持可移植 Node.js 安装目录和版本变量，避免强依赖某个发行版包源。
- 同时提供 Docker / Docker Compose 部署方式，容器内只运行动态服务，宿主机继续负责 nginx、Cloudflare 前置或反向代理。
- 部署脚本必须通过环境变量和交互输入接收真实域名、密码和 Token，不把真实服务器 IP、账号、密码、Token 或证书私钥写入仓库。
- 默认动态服务域名使用 `activity.20050619.xyz`，但应能通过环境变量改成其他域名。
- 动态数据目录应默认在仓库外，例如 `/var/lib/blog-dynamic`，迁移服务器时只需要迁移该数据目录和服务环境变量。
- 允许 Cloudflare 继续作为 DNS 和 HTTPS 前置；如果只配置 nginx 80 端口，需在文档中说明 Cloudflare 代理或 TLS 的关系。
- 动态服务公开地址使用 `https://` 时，动态服务服务器必须提供可用的 443 HTTPS 反向代理；部署脚本应支持自动识别 `/etc/letsencrypt/live/<域名>/` 证书、通过 `BLOG_DYNAMIC_LETSENCRYPT_EMAIL` 自动申请证书，或通过 `BLOG_DYNAMIC_SSL_CERT_PATH` / `BLOG_DYNAMIC_SSL_KEY_PATH` 使用已有证书。
- 如果 Cloudflare 代理阻止 Let's Encrypt HTTP-01 验证，可以使用服务器 origin TLS 兜底，但自签 origin 证书只允许在用户明确批准 Cloudflare `Full` 模式安全取舍后启用；更严格的生产方案应优先使用 Let's Encrypt、Cloudflare Origin CA 或其他受信任证书并配合 `Full strict`。
- 如果源站本机 nginx/TLS 正常，但公网通过动态服务域名 SNI 回源仍返回 525 或出现备案/上游策略拦截，应优先使用 Cloudflare Tunnel，让服务器主动出站连接 Cloudflare。Tunnel token 只能放在服务器本地文件中，例如 `/etc/blog-dynamic-cloudflared.token`，不得写入仓库、文档、前端构建产物或聊天记录。

## 17. 本次动态图片与日记本体验升级

用户确认动态和日记功能已可正常使用，本次继续完善交互与展示体验。

新增要求：

- 动态发布支持附带图片，支持把图片直接拖动到动态编写界面上传，也支持点击选择图片。
- 单条动态展示应以展示为主，动态编写区域不常驻显示在列表上方，改为右上角一个小的倾斜笔图标；点击图标后先输入发布密码，密码正确再打开动态编辑器。
- 日记入口触发条件改为 8 秒内连续点击互动小人 20 次。
- 日记密码弹窗打开后，点击弹窗外部区域不能关闭弹窗，避免快速点击时误关闭；只有明确点击关闭按钮才关闭。
- 动态内容较少时完整展示，例如 100 字以内文本、4 张以内图片；图片展示使用规整方格布局。
- 动态内容较多时显示“查看更多”按钮，点击后完整展开，并把按钮切换为“收起”。
- 进入日记功能后先展示日记本列表，再通过小按钮进入写日记界面。
- 日记本顶部需要搜索功能，可以根据标题或内容关键字查找日记。
- 所有新增图片和日记内容继续存储在独立动态服务服务器的数据目录，不写入 GitHub Pages 静态发布目录。

## 18. 本次动态与日记管理、检索和性能升级

用户确认当前 Cloudflare Tunnel、动态发布、日记和图片上传功能均已正常，本轮只在已有链路上增强展示、管理、检索和性能，不重新调整 Cloudflare、GitHub Pages 自定义域名或动态服务公开地址。

新增要求：

- 动态列表中每条动态不再默认显示“动态”标题；没有标题时只显示正文、图片和时间。
- 动态之间需要更清晰、有层次感的分界线，便于浏览时区分每条记录。
- 动态时间显示在单条动态右下角，字号更小，颜色改为蓝色。
- 动态图片在列表中统一显示为正方形缩略图；图片本身不裁切，非正方形部分使用透明或柔和背景补齐；点击缩略图后按原比例查看原图。
- 动态页面增加检索功能：可按标题关键字、正文关键字和起止日期筛选动态。
- 右上角小笔图标升级为动态管理入口：密码确认后可发布新动态，也可编辑或删除已有动态。
- 日记本增加编辑和删除功能，并在日记之间提供清晰分界线。
- 日记标题为非必填；没有标题的日记不显示“未命名日记”这类占位标题。
- 日记正文展示必须自动换行；内容较长时默认展示部分内容，并提供“查看更多 / 收起”切换。
- 编写动态和日记时应减少输入卡顿；输入框输入不应触发列表频繁重绘。
- 日记检索增加起止日期筛选，和关键字检索组合使用。
- 页面整体交互应尽量减少不必要 DOM 重绘、全量重渲染和同步重计算。
- 使用项目根目录本轮新增的 `banner.jpg` 作为动态页 Banner；公开访问时应位于 `public/images/banner/` 下，不引用根目录图片。

安全和部署边界：

- 本轮服务端新增的编辑、删除接口必须继续要求动态发布会话、日记会话或管理 Token，不允许无认证修改数据。
- 真实密码、Token、服务器地址、证书和 Tunnel token 仍只存在服务器或本地配置文件中，不写入仓库。
- 如果服务端接口新增后需要部署，仍通过既有 Docker / Cloudflare Tunnel 链路更新动态服务，不修改已验证可用的公网访问配置。

## 19. 本次服务器 Docker 启动兼容修复

当前独立动态服务服务器上只有旧版 `docker-compose`，没有可用的 Docker Compose v2 `docker compose` 插件；同时旧版 `docker-compose` 在新版 Docker 上重建既有容器时可能触发 `ContainerConfig` 兼容错误。

新增维护要求：

- 服务器部署说明不能只写 `docker compose`，需要同时说明旧服务器可使用 `docker-compose` 或推荐的一键脚本。
- 推荐更新服务时优先运行 `sudo bash /opt/blog-project/server/bootstrap-docker.sh`，由脚本处理容器重建、nginx 和 Cloudflare Tunnel 相关配置。
- 如果旧 `docker-compose` 重建容器失败，可以只删除 `blog-dynamic` 容器后用已构建镜像重新创建；不得删除 `/var/lib/blog-dynamic` 数据目录。
- Docker 原生命令 `docker run --env-file` 不会像 Compose 一样处理带引号的 env 值；部署脚本需要为 Docker 兜底启动写入无外层引号的专用 env 文件，服务端也应容错清理误带入的外层引号。
- Docker 容器仍只绑定 `127.0.0.1:8787`，公网访问继续走既有 Cloudflare Tunnel，不修改已经验证可用的 Cloudflare 和 GitHub Pages 配置。

## 20. 本次动态与日记展示回归修复

用户反馈动态和日记的展示样式、展开收起和管理交互仍未达到预期。本轮只修复前端展示与交互，不改动已经验证可用的 Cloudflare、GitHub Pages 自定义域名、动态服务公开地址和服务器公网入口。

新增修复要求：

- 动态列表中运行时插入的动态条目必须稳定套用样式，分界线、立体边框、右下角蓝色小时间、自动换行、正方形缩略图和展开收起效果必须在真实页面生效。
- 动态图片缩略图必须固定为正方形展示，图片本身不裁切，点击后继续按原比例打开原图。
- 动态长正文默认只展示部分内容，点击“查看更多”后完整展示，再点击“收起”后恢复折叠；按钮状态必须和内容状态一致。
- 动态管理弹窗必须能在可视区域内滚动，管理列表不应因为弹窗高度限制而无法继续向下查看。
- 动态编辑和删除按钮默认不显示；进入动态管理弹窗后，需要再点击“管理”进入管理模式，才显示编辑和删除按钮，退出管理模式后隐藏。
- 日记列表中运行时插入的日记条目必须稳定套用样式，清晰分界线、自动换行、长正文展开收起必须在真实页面生效。
- 日记编辑和删除按钮默认不显示；进入日记本后，需要点击“管理”进入管理模式，才显示编辑和删除按钮，退出管理模式后隐藏。
- 没有标题的日记继续不显示“未命名日记”等占位标题。
- 本轮如只修改前端页面，部署时应重新触发 GitHub Actions 发布静态站；不需要重建动态服务容器。

## 21. 本次动态编辑弹窗滚动修复

用户指出可参考项目根目录下的 `演示视频4.mp4`，当前进入动态编辑页面后仍无法上下滑动。本轮只修复前端弹窗交互，不改动已验证可用的动态服务、Cloudflare Tunnel、GitHub Pages 自定义域名或服务器部署链路。

新增修复要求：

- 动态管理 / 编辑弹窗打开后，即使内容高度超过当前屏幕，也必须能稳定上下滚动。
- 动态发布表单、图片拖拽区、图片预览、保存状态和底部管理列表都必须能通过滚动到达。
- 点击已有动态的“编辑”或点击“新动态”后，应自动把编辑器区域带回可视位置，避免用户卡在管理列表位置。
- 移动端触控滚动和桌面鼠标滚轮滚动都应可用。
- 本轮如果只修改前端页面，部署时只需要重新触发 GitHub Actions 发布静态站，不需要重建动态服务容器。
