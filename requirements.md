# 项目需求说明

## 1. 项目概述

本项目是一个个人静态博客网站。

当前目标是：简单、安全、维护成本低、方便迁移，并且适合以后逐步扩展动态功能。

当前主路线：

- 使用 Astro 生成静态博客
- 使用 GitHub Pages 托管公开网页
- 使用 Cloudflare 管理 DNS、自定义域名、HTTPS 和前置保护
- 阿里云 ECS 暂时只作为未来动态服务预留，不参与当前公开博客部署

当前阶段不要把本项目做成 WordPress、Ghost 或其他完整动态博客系统。

公开博客主体必须保持静态。

---

## 2. 网站基本信息

网站名称：

```text
辣子鸡丁砂锅
```

网站描述：

```text
这个人懒死了，什么介绍都没有
```

网站描述需要集中配置，方便以后修改。

作者昵称：

```text
辣子鸡丁砂锅
```

博客语言：

```text
中文
```

所有面向用户展示的默认文案应使用简体中文。

---

## 3. 已知项目信息

### 博客域名

```text
blog.20050619.xyz
```

### GitHub 账号

```text
JiLiGUaLaaaaa
```

### GitHub 仓库

```text
git@github.com:JiLiGuaLaaaaa/JiLiGuaLaaaaa.github.io.git
```

### 部署目标

公开博客部署到 GitHub Pages。

最终访问地址：

```text
https://blog.20050619.xyz
```

---

## 4. Cloudflare DNS 信息

Cloudflare 中已经确认：

```text
blog.20050619.xyz CNAME jiligualaaaaa.github.io
```

也就是说：

```text
blog.20050619.xyz
```

已经指向 GitHub Pages 对应的默认域名：

```text
jiligualaaaaa.github.io
```

项目文档中可以解释 CNAME 的含义，但不要写入任何 Cloudflare 密钥、Token 或账号密码。

---

## 5. 安全要求

严禁把任何敏感信息提交到 GitHub 仓库。

不要提交以下内容：

- 阿里云 ECS root 密码
- 阿里云 ECS 公网 IP 和密码组合
- SSH 私钥
- Cloudflare API Token
- GitHub Token
- `.env` 中的真实密钥
- 数据库文件
- 私密日记内容
- 本地保存的服务器登录信息
- 任何包含账号密码的文件
- 任何保存真实服务器连接信息的配置文件

`.gitignore` 至少应包含：

```gitignore
.env
.env.*
*.pem
*.key
server-secret.txt
secrets.txt
private/
diary/
*.sqlite
*.db
```

如果仓库里已经存在敏感信息，应立即停止继续开发，并明确提醒用户删除或清理。

如果本地配置文件曾经包含敏感信息，必须先脱敏或删除，再继续开发。脱敏后的文件只能保留安全说明或假占位内容，不能保留真实登录名、密码、Token、私钥、公网 IP 与密码组合等信息。

当前博客代码、文档、示例、构建产物和提交内容都必须严格避免泄露服务器信息、账号信息或密钥信息。

---

## 6. 当前架构

当前公开博客架构：

```text
Astro 静态博客
        |
        v
GitHub 仓库
        |
        v
GitHub Actions
        |
        v
GitHub Pages
        |
        v
Cloudflare DNS
        |
        v
https://blog.20050619.xyz
```

当前公开博客不需要：

- 阿里云 ECS
- Docker
- Docker Compose
- 数据库
- 服务端运行时
- WordPress
- Ghost
- 后端 API

---

## 7. 未来扩展架构

阿里云 ECS 暂时只作为未来动态服务预留。

未来可能使用 ECS + Docker / Docker Compose 部署以下服务：

- 评论系统
- 表单服务
- 访问统计
- API 服务
- 私密日记
- 其他自托管服务

未来动态服务可以使用独立子域名，例如：

```text
comment.20050619.xyz
form.20050619.xyz
stats.20050619.xyz
api.20050619.xyz
diary.20050619.xyz
```

这些未来动态服务不要在当前静态博客项目中实现，除非用户之后明确要求。

---

## 8. 技术栈

当前公开博客使用：

- Astro
- TypeScript
- Markdown 或 MDX
- GitHub Actions
- GitHub Pages
- Cloudflare 自定义域名

包管理器使用：

```text
pnpm
```

如果环境暂时无法使用 pnpm，可以说明原因，但优先使用 pnpm。

README 中需要用简单中文写清楚常用命令，不要求用户理解包管理器细节。

---

## 9. 页面要求

静态博客至少需要包含以下页面：

- 首页
- 博客列表页
- 博客文章详情页
- 关于页
- 标签页
- 归档页
- 搜索页或搜索入口
- 404 页面

博客文章公开路径格式必须使用：

```text
/blog/xxx/
```

不要使用：

```text
/posts/xxx/
```

除非用户之后明确修改需求。

---

## 10. 静态博客功能要求

需要实现或预留以下功能：

- 暗色模式
- RSS
- sitemap
- robots.txt
- 标签
- 归档
- 搜索
- 评论占位
- 右下角可爱的二次元小角色
- 鼠标移动时，小角色视线或头部跟随光标移动

说明：

- RSS 用于让读者订阅博客更新
- sitemap 用于帮助搜索引擎发现网站页面
- 标签用于按主题分类文章
- 归档用于按时间整理文章
- 搜索优先使用静态搜索方案，不要为了搜索引入后端服务

搜索功能优先考虑静态方案，例如 Pagefind。除非用户以后明确要求，不要使用服务端搜索。

---

## 11. 博客文章要求

博客文章使用 Markdown 或 MDX 编写。

文章 frontmatter 推荐格式：

```yaml
title: "文章标题"
description: "文章描述"
pubDate: 2026-01-01
updatedDate: 2026-01-02
tags:
  - 示例标签
draft: false
```

文章需要支持：

- 标题
- 描述
- 发布时间
- 更新时间
- 标签
- 草稿状态
- 代码高亮
- SEO 信息
- 响应式阅读布局

生产环境构建时，`draft: true` 的文章不应发布。

---

## 12. 设计要求

网站整体风格应当：

- 简洁
- 清爽
- 可爱
- 适合中文阅读
- 适合个人博客
- 移动端友好
- 加载速度快

必须支持：

- 暗色模式
- 响应式布局
- 良好的中文排版
- 清晰的导航
- 舒适的文章阅读宽度
- 足够的颜色对比度

当前视觉调整要求：

- 网站底色使用樱花粉色系
- 背景图使用用户提供的二次元雪景图，项目路径为 `public/images/site-bg.jpg`
- 背景图作为界面背景显示，提高不透明度到约 45%，同时使用内容区半透明遮罩保证正文可读
- 如果背景图暂时不存在，页面应使用樱花粉渐变兜底

可以加入但不要过度复杂化：

- 阅读进度条
- 返回顶部按钮
- 简单页面动画
- 轻量交互效果

---

## 13. 右下角二次元小角色要求

网站需要添加一个可爱的二次元风格小角色。

要求：

- 作为纯前端组件实现
- 不需要后端服务
- 不需要数据库
- 不需要 ECS
- 可以在 GitHub Pages 上正常运行
- 默认放在页面右下角附近
- 鼠标移动时，小角色的眼睛、头部或脸部应跟随光标移动
- 风格要可爱、轻量，不要影响阅读
- 移动端如果遮挡阅读，可以隐藏或简化
- 尊重 `prefers-reduced-motion`
- 不要为了这个功能引入复杂后端或动态博客系统

建议组件路径：

```text
src/components/CursorCharacter.astro
```

当前小角色调整要求：

- 小角色应比原 SVG 更好看，并保留轻量纯前端实现
- 增加更多交互：鼠标视线跟随、根据鼠标位置抬头/低头/左右转头形成侧脸感、点击问候、拖动位置、空闲眨眼或轻微浮动、点击动作反馈、隐藏/显示
- 当前交互小人改为使用用户补充的三张动作图切分出来的帧资源：九宫格视线方向、五帧眨眼、八帧打招呼动作
- 动作帧资源放在 `public/images/mascot-frames/`，包括 `look/`、`blink/` 和 `wave/`
- 小人交互应根据场景切换对应帧：鼠标位置切换视线方向帧，空闲时播放眨眼帧，点击或问候时播放打招呼帧
- 新版互动小人不再使用原来的 SVG 模型、旧单图模型或 CSS 旋转模型作为主要表现方式；图片帧缺失时才显示简洁兜底状态
- 动作帧切分必须统一人物缩放比例、画布尺寸和脚底/衣服底部基线，避免不同动作之间忽大忽小
- 视角帧必须二次审核，右看方向如果原图不合理，应基于左看帧镜像生成，保证左右方向成对一致
- 眨眼动作不能打断当前视角；只有在小人正面/闲置稳定状态下才播放眨眼，避免从其他方向突然跳回正面再跳走
- 打招呼动作应作为独立动作播放，进入和结束时回到合理的正面状态，避免与视线方向帧强行混切造成不连贯
- 眨眼动作应只改变眼部区域，身体、头发、衣服和整体轮廓应保持与正面空闲帧一致，避免整个人物在眨眼时跳动
- 打招呼动作帧必须检查切分边界，不能出现上一帧或下一帧的手部残片进入画面角落，手部应尽量完整保留
- 正上方和正下方视角应使用明确对应的动作帧，并调整鼠标方向判定，保证光标位于小人正上方或正下方时可以稳定触发
- 可通过本地算法为打招呼动作补充过渡帧，提升动画连贯性；眨眼可使用只覆盖眼部的前端动画保持身体不动；补帧应保持统一画布、统一基线和透明背景
- 眨眼闭合时必须遮住原本睁开的虹膜和眼白区域，不能只显示睫毛线而保留睁眼效果
- 打招呼动作应从静止正面状态自然进入挥手状态，身体、头部和服饰主体不应在挥手过程中大幅位移或缩放；优先让身体保持稳定，只让手臂/手部产生主要动作变化
- 动作帧下方服饰区域不能保留明显不规则透明破洞；应通过本地可复现的 alpha 修复或局部填补方式恢复衣服主体，不要用会泄露源图或敏感信息的外部服务处理
- 眨眼动作需要保留 `public/images/mascot-frames/blink/` 中半闭眼、闭眼帧的傲娇神情；播放速度不能过快，应使用更慢的帧序列表现自然眨眼，优先播放合成后的 PNG 帧序列，而不是再用整张人物切换
- 修补残缺衣服、头发或白边时，应优先参考最初用户提供的源图中对应帧的原始内容进行局部合成，不应使用粗暴扩张 alpha 造成新白边或不自然块状区域
- 眨眼表情不能把不匹配的人脸区域硬扣到当前角色脸上；应基于当前正面帧的人脸坐标局，只重绘或局部合成眼睛、眼皮和嘴部，使半闭眼、闭眼表情贴合当前脸型
- 打招呼动作不能只让手部单独运动；手部必须尽量完整显示，同时头部和身体应有轻微配合动作，形成自然的问候姿态，避免僵硬的静止身体叠加断手
- 眨眼动作的眼皮、睫毛和遮罩必须严格限制在眼部区域内，不能出现睫毛延伸到耳边、脸部大块破洞或闭眼后仍像睁眼的效果；生成后必须通过预览图二次审查
- 挥手动作帧必须保留完整手部，歪头帧必须保留完整头部和头发轮廓；切分、抠图、碎片清理和 alpha 修复不能误删主体部位，生成后必须逐帧二次审查
- 眨眼和招手播放前应尽量预加载并解码所有动作帧，前端切帧时不能因为图片未准备好、错误兜底反复显示或尺寸重排造成频繁闪烁
- 当前交互小人应优先改为使用用户最新补充的视频素材 `生成指定动作视频 (3).mp4` 处理得到的透明动作帧；需要从视频中重新抽取代表性动作帧，识别每帧对应的动作阶段，统一画布、缩放比例和底部基线后用于网页交互
- `public/images/video-mascot/` 下的当前主用小人帧必须基于 `生成指定动作视频 (3).mp4` 全量重新生成，不得继续复用旧视频 `(2)` 生成的方向帧、眨眼帧或挥手帧
- 视频素材中的绿色背景必须去除；应使用本地可复现的色键/HSV 绿幕抠图、边缘柔化和去绿边方法生成透明 PNG，不依赖外部服务
- 视频素材中的水印必须从最终互动小人资源中去除；优先通过水印区域检测、裁切避让、透明化或局部修复等本地算法处理，不能把明显水印保留进网页资源
- 视频处理后的帧必须生成本地预览图并二次审查，确认人物轮廓、动作分段、绿幕、水印和边缘质量，不合格帧不得作为网页主交互帧
- 视频小人抠图不能保留明显白边、灰边或绿边；应通过本地可复现的色键、主体保护、边缘去溢色、alpha 收缩/柔化和预乘色修正来改善轮廓，不得把大面积背景色残留进最终 PNG
- 视频小人的左右视角命名必须按用户视角和鼠标方向正确对应；如果原始抽帧方向与网页语义相反，应在生成脚本中统一纠正，不能只在运行时临时硬凑
- 鼠标移动触发视角切换时不能只在九张静态方向图之间硬切，应生成并播放更多中间方向帧或过渡帧，让左右、上下和斜向切换更连贯
- 视频小人的眨眼动作不能出现随机闪烁；眨眼序列必须与正面帧尺寸、透明边界和返回帧一致，并且前端播放时不能和视角切换、图片加载或错误兜底互相打断
- 视频小人的挥手帧不能把手臂、袖子或身体边缘误抠掉；如果色键或连通域清理删掉了主体局部，应通过局部 alpha 修复或参考相邻帧补回缺失区域，并生成预览图二次审查
- 视频小人的方向补帧不能只是从中心帧过渡到八方向帧；应生成更多带方向语义的角度帧，例如上偏左、上偏右、下偏左、下偏右等细分角度，并由前端按鼠标相对角度选择最接近的方向帧
- 视频小人的方向细分帧应优先从原视频连续动作段抽取稳定真实帧，缺少的对称方向可以用真实帧镜像生成；不得使用会产生肉眼可见残影的透明叠加、多图混合或插值方法
- 视频小人的挥手帧必须重点检查手指指尖，不能在色键抠图、去水印或 alpha 清理时把指尖削掉；如果指尖仍缺失，应通过更保守的肤色保护或局部 alpha 修复补回，并二次确认
- 点击一次小人时，打招呼动作只能播放一次伸手挥手序列，不能在结束时再反向播放一次导致像连续伸手两次；结束后应自然回到正面闲置帧
- 视频小人的方向细分帧不能使用会产生肉眼可见残影的透明叠加或多图混合；如果原视频中存在对应角度，应优先从原视频连续动作段抽取真实帧作为方向帧，缺少的左右对称角度可以用真实帧镜像生成
- 视频小人的方向细分帧必须是稳定单帧，不应出现双脸、双头发、半透明重影或多姿态叠加痕迹；同一动作帧不得机械重复填充多个角度，除非该角度确实是镜像对称帧并在审查图中标明来源
- 视频小人的 `wave-4`、`wave-5`、`wave-6` 以及所有挥手帧必须逐帧检查手指指尖、手掌边缘和服饰主体；指尖、手掌夹角、袖口和衣服不得被色键、去水印、裁切或连通域清理削掉
- 视频小人方向帧必须生成审查图，标出每个方向对应的源视频帧号、是否镜像、是否与其他帧重复，以及人物视线是否确实朝向对应方向；重复帧或方向不匹配帧不得作为最终主用方向帧
- 网页小人播放时必须避免闪烁：动作帧应预加载并解码，切帧时保持同一 `<img>` 尺寸、同一画布比例和透明边界；如果仍出现闪烁，应区分是资源未解码、帧 alpha 边界跳变、组件状态机打断还是浏览器缓存问题，并在代码中修正
- 移动端隐藏或简化，不遮挡正文
- 尊重 `prefers-reduced-motion`

如果使用第三方 Live2D 模型或图片资源，必须确认许可证允许使用。

如果许可证不明确，优先使用原创的 SVG / CSS / JavaScript 实现。

---

## 14. 评论占位要求

当前阶段需要添加评论区占位，但不要实现真实评论后端。

要求：

- 评论区域方便以后替换成真实评论系统
- 不连接 ECS
- 不连接数据库
- 不使用真实 API Key
- 不写入任何第三方评论服务密钥
- 不引入动态博客系统

评论占位可以显示类似文案：

```text
评论功能以后再加，现在先留个位置。
```

未来评论服务可能部署在：

```text
comment.20050619.xyz
```

---

## 15. 私密日记要求

私密日记不能放在当前 GitHub Pages 静态博客中。

不要创建公开的私密日记页面，例如：

```text
/diary
/private
/private-diary
```

除非用户明确说明这些内容可以公开。

如果以后要做私密日记，应作为单独服务部署在 ECS 上，并加访问控制，例如：

- Cloudflare Access
- Basic Auth
- 应用自身登录
- VPN
- Tailscale
- WireGuard

未来私密日记可能使用：

```text
diary.20050619.xyz
```

---

## 16. 部署要求

使用 GitHub Actions 部署到 GitHub Pages。

工作流文件建议路径：

```text
.github/workflows/deploy.yml
```

部署流程应包括：

- 安装依赖
- 构建 Astro 项目
- 上传静态产物
- 发布到 GitHub Pages

Astro 配置中的站点地址应为：

```text
https://blog.20050619.xyz
```

当前公开博客不要使用 ECS 部署。

当前公开博客不要使用 Docker 部署。

GitHub Pages 的 Build and deployment Source 必须选择 GitHub Actions。不要使用 Deploy from a branch 直接发布 Astro 源码，否则 GitHub Pages 会按 Jekyll 解析 `.astro` 文件并导致 YAML front matter 错误。

---

## 17. 文档要求

项目需要包含中文 README。

README 应使用简单中文说明：

- 这个项目是什么
- 为什么使用静态博客方案
- 如何本地运行
- 如何写新文章
- 如何构建
- 如何部署到 GitHub Pages
- Cloudflare 和自定义域名如何配置
- CNAME 是什么
- RSS 是什么
- sitemap 是什么
- 标签、归档、搜索是什么
- 哪些文件不能提交
- 为什么当前公开博客不使用 ECS
- 未来如何把评论、统计、表单、私密日记等服务单独放到 ECS

---

## 18. 需求变更管理要求

每次需求发生变化，都必须修改本文件。

不要只改代码而不改需求文档。

以下情况都属于需求变化：

- 新增页面
- 删除页面
- 修改设计风格
- 新增组件
- 修改部署方式
- 修改域名
- 新增评论、统计、搜索、表单等功能
- 修改 ECS / Docker / Cloudflare / GitHub Pages 相关策略
- 修改安全规则
- 修改私密日记策略

每次需求变化时，必须按顺序执行：

1. 更新 `requirements.md`
2. 更新或创建 `plan.md`
3. 再开始实现代码
4. 必要时更新 README
5. 运行检查
6. 完成后更新 plan 状态

---

## 19. 计划管理要求

开始工程前，必须先创建 `plan.md`。

`plan.md` 必须使用 Markdown checkbox 形式，例如：

```md
# Plan

- [ ] 阅读 requirements.md
- [ ] 检查当前仓库结构
- [ ] 创建 Astro 项目结构
- [ ] 添加 GitHub Pages 部署配置
- [ ] 运行构建检查
```

每完成一个计划项，就把对应项改成：

```md
- [x] 已完成的任务
```

每次开始新的计划项之前，都必须完整阅读一遍 `plan.md`。

不要跳过此步骤。

---

## 20. 质量检查要求

在认为任务完成前，需要运行可用检查。

优先使用：

```bash
pnpm install
pnpm build
```

如果添加了 lint 或格式化工具，也需要运行：

```bash
pnpm lint
pnpm format
```

如果命令失败，应先修复问题，再把对应 plan 项标记为完成。

不要在构建失败时声称任务已完成。

---

## 21. 非目标

除非用户之后明确要求，否则不要实现：

- WordPress
- Ghost
- 服务端渲染博客
- 数据库 CMS
- 用户登录系统
- 公开博客后台管理系统
- 把私密日记放进 GitHub Pages
- 当前仓库中的动态后端 API
- 当前公开博客的 Docker 部署
- 当前公开博客的 ECS 部署
